const EventCard = (props) => {
  // {
  // name,
  // image,
  // location
  // date
  // deadline
  //Fees
  // }
  const { event } = props;
  return <CardStyle>
    <ImageConatiner>

    </ImageConatiner>
    <CardContent>
        <title>
    </CardContent>
  </CardStyle>;
};

export default EventCard;
